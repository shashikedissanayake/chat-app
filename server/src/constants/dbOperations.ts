export enum DbOperations {
    PUT = 'Put',
    UPDATE = 'Update',
    UPDATE_EXPRESSION = 'Update_exp',
    GET = 'Get',
    DELETE = 'Delete',
}

export enum UPDATE_EXPRESSION_OPERATIONS {
    ADD = '+',
    SUBSTRACT = '-',
}
