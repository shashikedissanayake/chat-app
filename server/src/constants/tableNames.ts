export enum TableTypes {
    CHAT_TABLE = 'Chat',
}

export enum RecordTypes {
    USERS = 0,
    ROOMS = 1,
    MESSAGES = 2,
}

export enum DBPrefixes {
    USERS = 'user',
    ROOMS = 'room',
    MESSAGES = 'message',
    SEEN = 'seen',
}
